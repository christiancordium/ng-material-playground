import * as Factory from 'factory.ts';
import * as faker from 'faker';
import * as _ from 'lodash';

// export const PlanFactory = Factory.makeFactory<Cordium.IPlan>({
// 	id: Factory.each(() => 'PLAN-' + faker.lorem.slug(3).toUpperCase()),
// 	name: Factory.each(() => faker.lorem.sentence()),
// 	description: Factory.each(() => faker.lorem.sentence()),
// 	teamVisas: [ '1' ],
// 	series: [],
// 	permissions: [1, 2, 4, 8],
// 	active: true
// });
